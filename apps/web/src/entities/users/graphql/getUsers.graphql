query GetUsers($pagination: PaginationInput!) {
  findPaginatedUserType(pagination: $pagination) {
    items {
      id
      username
      role
      createdAt
    }
    total
    page
    totalPages
  }
}
